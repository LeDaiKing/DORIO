### init

Use the following to build for desktop:

``` bash
cmake -S . -B build
cmake --build build
```

### build
``` bash
cmake --build build
```

### run
``` bash
./build/run
or
build/run.exe
```


